# Harbour targets Node 24 LTS (see "engines" in package.json). engine-strict
# makes `npm install` fail loudly on an unsupported Node instead of warning,
# so contributors don't silently build the better-sqlite3 native binary
# against the wrong Node ABI.
engine-strict=true
